<isscript>
    var assets = require('*/cartridge/scripts/assets.js');
    assets.addCss('/css/components/pd-statementtext.css');
</isscript>

<section class="statement-text ${pdict.containerWidth}">
    <div class="statement-text__content${pdict.vposition}${pdict.hposition}${pdict.textpos}">
        <isprint value="${pdict.regions.title.render()}" encoding="off" />
        <isprint value="${pdict.regions.body.render()}" encoding="off" />
    </div>
</section>