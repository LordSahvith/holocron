<isscript>
    var assets = require('*/cartridge/scripts/assets.js');
    assets.addCss('/css/components/pd-gridAndTile.css');
</isscript>
<div class="grid-cards-wrapper">
    <isif condition="${!empty(pdict.gridHeader)}">
        <h2 class="cards-grid-header">${pdict.gridHeader}</h2>
    </isif>
    <isprint value="${pdict.regions.cards.render()}" encoding="off" />
</div>
